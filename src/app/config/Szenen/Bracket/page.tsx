import Layout from "@/components/layout";

export default function ConfigPage() {
    return (
        <Layout>
            <main className="flex items-center gap-12 h-screen">
                <div className="w-full h-[85%] pb-8 p-2">
                    <span>SZENEN/BRACKET</span>
                </div>
            </main>
        </Layout>
    );
}